version: '3.7'

services:
  ganache:
    image: trufflesuite/ganache:latest
    restart: always
    ports:
      - 8555:8555
    command: --db /data --accounts 16 --wallet.seed 20070213 --port 8555
    volumes:
      - "ganache-vol:/data"
volumes:
  ganache-vol: